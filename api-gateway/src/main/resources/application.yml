server:
  port: ${gateway.port}

spring:
  application:
    name: ${gateway.name}
  profiles:
    active: development
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka